

describe("ShoppingCart class", () => {

  describe("addItem()", () => {
    it("should add a new item to the cart", () => {});
  });

  describe("getTotal()", () => {
    it("should calculate total price correctly when the cart is not empty", () => {});
    it("total price should be 0 when the cart is empty", () => {});
  });

  describe("checkout()", () => {
    it("should start payment process with total amount", () => {});

    it("should not start the payment process if cart is empty", () => {});
  });
});
